# Download and quick run

Dependencies are:
- netcdf (need libnetcdf.a or libnetcdf.so)
- gmt (optional, for plotting)

A easy start:

```
$ git clone --recursive https://github.com/Junbiao-Liu/seismic-tomography-models-new.git
$ cd ./seismic-tomography-models/src
$ make
$ ./example.out
```
